{
  "name": "retro",
  "version": "1.0.0",
  "description": "A retro js framework",
  "license": "ISC",
  "author": "Godmar Back",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:retro && npm run build:app",
    "build:retro": "tsc -p src/retro/tsconfig.json",
    "build:app": "tsc -p src/app/tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
